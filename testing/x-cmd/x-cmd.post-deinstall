#!/bin/sh

post_remove() {
    local _pkgsum=.369a6a4b
    local _pkgfile="/usr/share/x-cmd/v/$_pkgsum.tgz"
    [ ! -f "$_pkgfile" ]    || rm -rf "$_pkgfile"
    [ ! -f /usr/bin/x-cmd ] || rm -rf /usr/bin/x-cmd
    [ ! -f /usr/bin/x ]     || rm -rf /usr/bin/x
}

post_remove

